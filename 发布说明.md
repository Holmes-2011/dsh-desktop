# DeepSeek Harness 本地版 0.2.0

完全自包含的 macOS 桌面版 DeepSeek Harness —— 内置 Node 运行时与 dsh 后端,双击即用,**无需安装任何其他软件**。

## 下载

| 文件 | 说明 |
|---|---|
| `DeepSeek Harness 本地版-0.2.0-macOS-arm64.dmg` | 安装映像(推荐):双击 → 拖入 Applications |
| `DeepSeek Harness 本地版-0.2.0-macOS-arm64.zip` | 备用格式:解压 → 拖入 Applications |

仅支持 **Apple 芯片 (arm64)**,macOS 14 及以上。

## 安装方法

1. 下载 `.dmg` 并双击打开,把 `DeepSeek Harness 本地版.app` 拖进 Applications。
2. 首次打开若提示"无法验证开发者":右键 app → **打开** → 确认(仅第一次需要,因应用为本地自签名,未做 Apple 公证)。
3. 打开后即进入 DeepSeek Harness 界面,聊天数据保存在本机 `~/.dsh`。

## 与网页版的关系

- **本地版**(本发布):完全自包含,推荐日常使用。
- **网页版**:旧版桌面壳,依赖系统全局安装的 node / dsh,仅作兼容保留。

## 更新记录

### 0.2.0
- 改为完全自包含:内置 Node v24 运行时 + 完整 dsh 运行时,零外部依赖
- 增加 `--resolve` 自检模式,便于排障
- 窗口标题与应用名带"本地版"后缀,与网页版区分
